name: Release (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: |
          echo "This workflow is deprecated."
          echo "Use nightly.yml, publish-stable.yml and release-please.yml instead."
